# Lab Daemon Single File Server Architecture (Legacy)

This folder is reserved for reference files and historical diagrams of the single file-server architecture where local disk storage was directly attached to the web and API VM. 
